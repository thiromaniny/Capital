//
//  HW7App.swift


import SwiftUI

// Marks the main entry point of the application
@main
struct HW7App: App {
    // Defines the main content of the application
    var body: some Scene {
        // Creates a window group for the app's user interface
        WindowGroup {
            // Sets the initial view of the app to be ContentView
            ContentView()
        }
    }
}
