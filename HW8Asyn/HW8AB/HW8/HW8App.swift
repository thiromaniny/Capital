//A&B
//  HW8App.swift
//
import SwiftUI // Import the SwiftUI framework, which is used for building the user interface of the app

// Define the main entry point of the app
@main
struct HW8App: App {
    // Define the body property of the app, which specifies the scene to display
    var body: some Scene {
        // Create a WindowGroup, which is a container for one or more windows
        WindowGroup {
            // Set ContentView as the initial view displayed in the window
            ContentView()
        }
    }
}

